using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class background : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        var width = Screen.width;
        var height = Screen.width * (5 / 4);
	}
}
